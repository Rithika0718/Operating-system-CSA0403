#include <stdio.h>

int main() {
    int start, len, i;
    printf("Enter start block & length: ");
    scanf("%d%d", &start, &len);

    printf("Blocks allocated:\n");
    for (i = start; i < start + len; i++)
        printf("%d ", i);
    return 0;
}
